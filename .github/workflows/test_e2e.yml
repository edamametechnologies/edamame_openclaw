name: E2E tests (retired)

# Retired in EDAMAME 1.7.0. Level-2 agent plugin E2E no longer gates release.
# Fleet monitoring gate: agent_security/.github/workflows/agent_monitoring_e2e.yml

on:
  workflow_dispatch:

jobs:
  retired:
    name: Retired (EDAMAME 1.7.0)
    runs-on: ubuntu-latest
    steps:
      - name: Retired notice
        run: |
          echo "Retired in EDAMAME 1.7.0: Level-2 agent plugin E2E no longer release-gates."
          echo "Fleet monitoring gate: edamametechnologies/agent_security agent_monitoring_e2e.yml"
          echo "Host-side observation is the default path; nono/srt provide prevention."
