name: Sign PowerShell scripts (retired)

# Retired in EDAMAME 1.7.0. Plugin signing no longer gates release.

on:
  workflow_dispatch:

jobs:
  retired:
    name: Retired (EDAMAME 1.7.0)
    runs-on: ubuntu-latest
    steps:
      - name: Retired notice
        run: |
          echo "Retired in EDAMAME 1.7.0: plugin PowerShell signing no longer release-gates."
          echo "release_agents.sh and tools/sign_agents.sh are stubbed with the same retirement message."
