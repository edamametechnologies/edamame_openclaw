{
  "name": "edamame-openclaw",
  "version": "1.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test tests/*.test.ts",
    "e2e:inject": "bash tests/e2e_inject_intent.sh"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
